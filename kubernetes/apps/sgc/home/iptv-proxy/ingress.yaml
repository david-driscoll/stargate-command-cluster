---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/refs/heads/main/traefik.io/ingressroute_v1alpha1.json
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: tivi-icon-ingress
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`tivi2.${ROOT_DOMAIN}`)
      kind: Rule
      services:
        - name: minio
          namespace: sgc
          port: api
          passHostHeader: true
      middlewares:
        - name: redirect-tivi-files
          namespace: sgc
    - match: Host(`iptv2.${ROOT_DOMAIN}`) && PathPrefix(`/icons/`)
      services:
        - name: minio
          namespace: sgc
          port: api
          passHostHeader: true
      middlewares:
        - name: redirect-tivi-files
          namespace: sgc
