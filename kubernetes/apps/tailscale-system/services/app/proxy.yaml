---
apiVersion: tailscale.com/v1alpha1
kind: ProxyClass
metadata:
  name: proxy
spec:
  metrics:
    enable: true
    serviceMonitor:
      enable: true
  # staticEndpoints:
  #   nodePort:
  #     ports:
  #       - port: 31667
  #         endPort: 31680
  #     selector:
  #       kubernetes.io/os: linux
