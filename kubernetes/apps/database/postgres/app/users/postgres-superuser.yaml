---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/refs/heads/main/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: &name postgres-superuser
  annotations:
    reloader.stakater.com/auto: "true"
spec:
  refreshPolicy: Periodic
  refreshInterval: 10m
  secretStoreRef:
    kind: ClusterSecretStore
    name: database
  target:
    name: *name
    creationPolicy: Owner
    deletionPolicy: Delete
    template:
      metadata:
        labels:
          cnpg.io/reload: "true"
        annotations:
          reloader.stakater.com/auto: "true"
      data:
        username: "postgres"
        password: "{{ .password }}"
        host: "postgres-rw.${NAMESPACE}.svc.cluster.local"
        port: "5432"
        database: "postgres"
        pgpass: "postgres-rw.${NAMESPACE}.svc.cluster.local:5432:postgres:postgres:{{ .password }}"
        jdbc-uri: "jdbc:postgresql://postgres-rw.${NAMESPACE}.svc.cluster.local:5432/postgres?password={{ .password }}&user=postgres"
        uri: "postgresql://postgres:{{ .password }}@postgres-rw.${NAMESPACE}.svc.cluster.local:5432/postgres"
  dataFrom:
    - extract:
        key: "postgres-superuser-password"
