apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: minio-config
    files:
      - values.yaml=values.yaml
    options:
      disableNameSuffixHash: true
resources:
  - authentik.yaml