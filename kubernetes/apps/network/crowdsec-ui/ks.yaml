---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/refs/heads/main/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app crowdsec-ui
  namespace: &namespace network
  labels:
    app.kubernetes.io/name: *app
    driscoll.dev/name: *app
spec:
  targetNamespace: *namespace
  commonMetadata:
    labels:
      app.kubernetes.io/name: *app
      driscoll.dev/name: *app
  dependsOn:
    # crowdsec's ks.yaml is wait: true, so this only starts once the LAPI is
    # actually Ready -- the init container below registers a watcher against it.
    - name: crowdsec
      namespace: network
    - name: external-secrets
      namespace: kube-system
    - name: volsync
      namespace: volsync-system
  path: ./kubernetes/apps/network/crowdsec-ui
  prune: true
  force: false
  wait: true
  interval: 1h
  retryInterval: 2m
  timeout: 10m
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  components:
    - ../../../components/failover/fast-node-eviction
    - ../../../components/tailscale
    - ../../../components/volsync
  postBuild:
    substitute:
      APP: *app
      NAMESPACE: *namespace
      # SQLite plus the GeoNames snapshot plus crowdsec.sync.lookback (168h of
      # alert history) is the size driver.
      VOLSYNC_CAPACITY: 5Gi
      VOLSYNC_ACCESSMODES: ReadWriteOnce
      # Sizes the long-lived ReplicationSource restic cache; 2Gi is what every
      # other volsync-backed app on this cluster runs with. Kept pinned rather
      # than left to the default because both movers read this one variable and
      # their defaults differ -- 2Gi in the ReplicationSource but 8Gi in the
      # ReplicationDestination -- so if components/volsync-restore is ever added
      # back here for a disaster-recovery restore, it inherits 2Gi instead of
      # provisioning an 8Gi 2-replica longhorn-cache volume.
      VOLSYNC_CACHE_CAPACITY: 2Gi
      # The image runs as the `node` user (1000:1000); the restic mover has to
      # match or it cannot read what it is backing up.
      VOLSYNC_PUID: "1000"
      VOLSYNC_PGID: "1000"
