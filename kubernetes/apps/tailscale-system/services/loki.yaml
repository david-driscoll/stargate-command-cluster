# Generated by Update.cs — do not edit manually
# Tailscale service: loki
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/refs/heads/master/v1.34.2/service.json
apiVersion: v1
kind: Service
metadata:
  name: loki
  annotations:
    tailscale.com/tailnet-fqdn: "loki.${TAILSCALE_DOMAIN}"
    tailscale.com/proxy-group: tailnet-inbound
spec:
  type: ExternalName
  externalName: loki
  ports:
    - name: http
      port: 3100
      targetPort: 3100
---
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: loki
spec:
  interval: 5m
  module: http_2xx
  prober:
    url: blackbox-exporter.observability.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - http://loki.${TAILSCALE_DOMAIN}:3100
