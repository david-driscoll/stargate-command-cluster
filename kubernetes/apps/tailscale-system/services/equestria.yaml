# Generated by Update.cs — do not edit manually
# Server: equestria  |  Services: Dns
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/refs/heads/master/v1.34.2/service.json
apiVersion: v1
kind: Service
metadata:
  name: dns-equestria
  annotations:
    tailscale.com/tailnet-fqdn: "dns-equestria.${TAILSCALE_DOMAIN}"
    tailscale.com/proxy-group: tailnet-inbound
spec:
  type: ExternalName
  externalName: dns-equestria
  ports:
    - name: dns-tcp
      port: 53
      targetPort: 53
    - name: dns-udp
      port: 53
      targetPort: 53
      protocol: UDP
    - name: dot
      port: 853
      targetPort: 853
    - name: doq
      port: 853
      targetPort: 853
      protocol: UDP
    - name: doh
      port: 443
      targetPort: 443
    - name: admin
      port: 53443
      targetPort: 53443
---
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: dns-equestria
spec:
  interval: 2m
  module: dns_soa
  prober:
    url: blackbox-exporter.observability.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - dns-equestria.${TAILSCALE_DOMAIN}:53
---
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: dns-equestria-dot
spec:
  interval: 2m
  module: tcp_connect
  prober:
    url: blackbox-exporter.observability.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - dns-equestria.${TAILSCALE_DOMAIN}:853
---
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: dns-equestria-doh
spec:
  interval: 2m
  module: http_2xx
  prober:
    url: blackbox-exporter.observability.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - https://dns-equestria.${TAILSCALE_DOMAIN}
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: dns-equestria-alerts
spec:
  groups:
    - name: dns-equestria
      rules:
        - alert: TechnitiumDnsUnhealthy
          annotations:
            description: "Technitium DNS on equestria is not answering SOA queries."
            summary: "Technitium DNS equestria unhealthy"
          expr: |
            probe_success{probe="dns-equestria"} < 1
          for: 10m
          labels:
            severity: critical
