apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ./secret.yaml
- ./helmrelease.yaml
- ./cluster-user.yaml
configMapGenerator:
- name: minio-values
  files:
  - values.yaml=./values.yaml
  options:
    disableNameSuffixHash: true
