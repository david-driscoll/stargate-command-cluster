# Generated by Update.cs — do not edit manually
# Server: alpha-site  |  Services: Proxmox
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/refs/heads/master/v1.34.2/service.json
apiVersion: v1
kind: Service
metadata:
  name: proxmox-alpha-site
  annotations:
    tailscale.com/tailnet-fqdn: "alpha-site.${TAILSCALE_DOMAIN}"
    tailscale.com/proxy-group: tailnet-inbound
spec:
  type: ExternalName
  externalName: alpha-site
  ports:
    - name: pve
      port: 8006
      targetPort: 8006
    - name: ssh
      port: 22
      targetPort: 22
    - name: nut
      port: 3493
      targetPort: 3493
---
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: proxmox-alpha-site
spec:
  interval: 5m
  module: http_2xx
  prober:
    url: blackbox-exporter.observability.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - https://alpha-site.${TAILSCALE_DOMAIN}:8006
---
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: proxmox-alpha-site-ssh
spec:
  interval: 5m
  module: ssh_banner
  prober:
    url: blackbox-exporter.observability.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - alpha-site.${TAILSCALE_DOMAIN}:22
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: proxmox-alpha-site-alerts
spec:
  groups:
    - name: proxmox-alpha-site
      rules:
        - alert: ProxmoxServiceUnhealthy
          annotations:
            description: "Proxmox VE on alpha-site is unhealthy."
            summary: "Proxmox alpha-site is unhealthy"
          expr: |
            probe_success{probe="proxmox-alpha-site"} < 1
          for: 10m
          labels:
            severity: warning
        - alert: ProxmoxSSHConnectivityLost
          annotations:
            description: "SSH connectivity to Proxmox on alpha-site has been lost."
            summary: "Proxmox alpha-site SSH lost"
          expr: |
            probe_success{probe="proxmox-alpha-site-ssh"} < 1
          for: 10m
          labels:
            severity: warning
