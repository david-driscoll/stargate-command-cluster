---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: prometheus-blackbox-exporter-secrets
spec:
  refreshInterval: 15m
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: prometheus-blackbox-exporter-secrets
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        # Add any secret values needed for prometheus-blackbox-exporter
  dataFrom: []