---
apiVersion: tailscale.com/v1alpha1
kind: ProxyGroup
metadata:
  name: &name "${CLUSTER_CNAME}-kubeproxy"
  annotations:
    reloader.stakater.com/auto: "true"
spec:
  type: kube-apiserver
  kubeAPIServer:
    mode: auth
  tags:
    - "tag:k8s"
    - "tag:${CLUSTER_CNAME}"
