---
# yaml-language-server: $schema=https://raw.githubusercontent.com/david-driscoll/stargate-command-cluster/refs/heads/main/schemas/definition.schema.json
apiVersion: driscoll.dev/v1
kind: ApplicationDefinition
metadata:
  name: ${APP}
spec:
  name: Weave GitOps
  category: ${CLUSTER_TITLE}
  description: Access to the Weave dashboard
  url: &url https://gitops.${CLUSTER_DOMAIN}
  # TODO: Change me someday
  icon: https://raw.githubusercontent.com/weaveworks/weave-gitops/refs/heads/main/ui/images/logoDark.svg
  access_policy:
    groups:
      - admins
  # authentik:
  #   proxy:
  #     mode: "forward_single"
  #     externalHost: *url
  uptime:
    http:
      url: *url
      method: GET
      accepted_statuscodes: ["200"]
      parent_name: "cluster-apps"
