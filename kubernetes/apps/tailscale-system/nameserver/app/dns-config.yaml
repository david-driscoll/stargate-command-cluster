---
apiVersion: tailscale.com/v1alpha1
kind: DNSConfig
metadata:
  name: ts-dns
  annotations:
    reloader.stakater.com/auto: "true"
spec:
  nameserver:
    service:
      clusterIP: 
    image:
      repo: tailscale/k8s-nameserver
      tag: unstable
