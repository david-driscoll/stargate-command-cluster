---
apiVersion: tailscale.com/v1alpha1
kind: ProxyGroup
metadata:
  name: inbound
spec:
  type: egress
  hostnamePrefix: ${CLUSTER_CNAME}-inbound
  replicas: 2
  tags:
    - "tag:egress"
    - "tag:management"
    - "tag:observability"
  proxyClass: default
