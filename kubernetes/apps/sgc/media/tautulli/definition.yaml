---
# yaml-language-server: $schema=https://github.com/david-driscoll/stargate-command-cluster/blob/main/schemas/definition.schema.json
apiVersion: driscoll.dev/v1
kind: ApplicationDefinition
metadata:
  name: ${APP}
spec:
  name: Tautulli (Cheyenne)
  category: Media Management
  uptime:
    http:
      url: https://${APP}.${CLUSTER_DOMAIN}/status
      method: GET
      accepted_statuscodes: ["200"]
      parent_name: "apps"
