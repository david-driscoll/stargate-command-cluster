---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/refs/heads/master/v1.34.2/service.json
apiVersion: v1
kind: Service
metadata:
  annotations:
    tailscale.com/tailnet-fqdn: "loki.${TAILSCALE_DOMAIN}"
    tailscale.com/tags: "tag:observability"
  name: loki-headless
spec:
  externalName: unused
  type: ExternalName
  ports:
    - port: 3100
      targetPort: 3100
      name: http
