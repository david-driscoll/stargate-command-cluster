---
# yaml-language-server: $schema=https://github.com/david-driscoll/stargate-command-cluster/blob/main/schemas/kuma.schema.json
apiVersion: autokuma.bigboot.dev/v1
kind: KumaEntity
metadata:
  name: apps
spec:
  config:
    name: Applications
    type: group
    interval: 300
    retry_interval: 60
    max_retries: 3

---
# yaml-language-server: $schema=https://github.com/david-driscoll/stargate-command-cluster/blob/main/schemas/kuma.schema.json
apiVersion: autokuma.bigboot.dev/v1
kind: KumaEntity
metadata:
  name: system
spec:
  config:
    name: System
    type: group
    interval: 300
    retry_interval: 60
    max_retries: 3

# ---
# # yaml-language-server: $schema=https://github.com/david-driscoll/stargate-command-cluster/blob/main/schemas/kuma.schema.json
# apiVersion: autokuma.bigboot.dev/v1
# kind: KumaEntity
# metadata:
#   name: networking
# spec:
#   config:
#     name: Networking
#     type: group
#     interval: 300
#     retry_interval: 60
#     max_retries: 3

# ---
# # yaml-language-server: $schema=https://github.com/david-driscoll/stargate-command-cluster/blob/main/schemas/kuma.schema.json
# apiVersion: autokuma.bigboot.dev/v1
# kind: KumaEntity
# metadata:
#   name: observability
# spec:
#   config:
#     name: Observability
#     type: group
#     interval: 300
#     retry_interval: 60
#     max_retries: 3

