name: authentik-pulumi
description: Authentik
runtime: dotnet
#backend:
#  url: postgres://
packages:
  authentik:
    source: terraform-provider
    version: 0.12.0
    parameters:
      - goauthentik/authentik
  uptimekuma:
    source: terraform-provider
    version: 0.12.0
    parameters:
      - kill3r-queen/uptimekuma
