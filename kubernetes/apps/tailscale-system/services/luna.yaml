# Generated by Update.cs — do not edit manually
# Server: luna  |  Services: Dockge, Proxmox, Pbs
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/refs/heads/master/v1.34.2/service.json
apiVersion: v1
kind: Service
metadata:
  name: dockge-luna
  annotations:
    tailscale.com/tailnet-fqdn: "dockge-luna.${TAILSCALE_DOMAIN}"
    tailscale.com/proxy-group: tailnet-inbound
spec:
  type: ExternalName
  externalName: dockge-luna
  ports:
    - name: https
      port: 443
      targetPort: 443
    - name: ssh
      port: 22
      targetPort: 22
---
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: dockge-luna-https
spec:
  interval: 5m
  module: http_2xx
  prober:
    url: blackbox-exporter.observability.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - https://dockge-luna.${TAILSCALE_DOMAIN}
---
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: dockge-luna-ssh
spec:
  interval: 5m
  module: ssh_banner
  prober:
    url: blackbox-exporter.observability.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - dockge-luna.${TAILSCALE_DOMAIN}:22
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: dockge-luna-alerts
spec:
  groups:
    - name: dockge-luna
      rules:
        - alert: DockgeServiceUnhealthy
          annotations:
            description: "Dockge service on luna is unhealthy."
            summary: "Dockge luna is unhealthy"
          expr: |
            probe_success{probe="dockge-luna-https"} < 1
          for: 10m
          labels:
            severity: warning
        - alert: DockgeSSHConnectivityLost
          annotations:
            description: "SSH connectivity to Dockge on luna has been lost."
            summary: "Dockge luna SSH lost"
          expr: |
            probe_success{probe="dockge-luna-ssh"} < 1
          for: 10m
          labels:
            severity: warning
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/refs/heads/master/v1.34.2/service.json
apiVersion: v1
kind: Service
metadata:
  name: proxmox-luna
  annotations:
    tailscale.com/tailnet-fqdn: "luna.${TAILSCALE_DOMAIN}"
    tailscale.com/proxy-group: tailnet-inbound
spec:
  type: ExternalName
  externalName: luna
  ports:
    - name: pve
      port: 8006
      targetPort: 8006
    - name: ssh
      port: 22
      targetPort: 22
---
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: proxmox-luna
spec:
  interval: 5m
  module: http_2xx
  prober:
    url: blackbox-exporter.observability.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - https://luna.${TAILSCALE_DOMAIN}:8006
---
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: proxmox-luna-ssh
spec:
  interval: 5m
  module: ssh_banner
  prober:
    url: blackbox-exporter.observability.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - luna.${TAILSCALE_DOMAIN}:22
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: proxmox-luna-alerts
spec:
  groups:
    - name: proxmox-luna
      rules:
        - alert: ProxmoxServiceUnhealthy
          annotations:
            description: "Proxmox VE on luna is unhealthy."
            summary: "Proxmox luna is unhealthy"
          expr: |
            probe_success{probe="proxmox-luna"} < 1
          for: 10m
          labels:
            severity: warning
        - alert: ProxmoxSSHConnectivityLost
          annotations:
            description: "SSH connectivity to Proxmox on luna has been lost."
            summary: "Proxmox luna SSH lost"
          expr: |
            probe_success{probe="proxmox-luna-ssh"} < 1
          for: 10m
          labels:
            severity: warning
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/refs/heads/master/v1.34.2/service.json
apiVersion: v1
kind: Service
metadata:
  name: pbs-luna
  annotations:
    tailscale.com/tailnet-fqdn: "luna.${TAILSCALE_DOMAIN}"
    tailscale.com/proxy-group: tailnet-inbound
spec:
  type: ExternalName
  externalName: luna
  ports:
    - name: pbs
      port: 8007
      targetPort: 8007
---
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: pbs-luna
spec:
  interval: 5m
  module: http_2xx
  prober:
    url: blackbox-exporter.observability.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - https://luna.${TAILSCALE_DOMAIN}:8007
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: pbs-luna-alerts
spec:
  groups:
    - name: pbs-luna
      rules:
        - alert: PBSServiceUnhealthy
          annotations:
            description: "Proxmox Backup Server on luna is unhealthy."
            summary: "PBS luna is unhealthy"
          expr: |
            probe_success{probe="pbs-luna"} < 1
          for: 10m
          labels:
            severity: warning
