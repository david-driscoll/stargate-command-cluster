# Generated by Update.cs — do not edit manually
# Server: celestia  |  Services: Dockge, Proxmox, Pbs
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/refs/heads/master/v1.34.2/service.json
apiVersion: v1
kind: Service
metadata:
  name: dockge-celestia
  annotations:
    tailscale.com/tailnet-fqdn: "dockge-celestia.${TAILSCALE_DOMAIN}"
    tailscale.com/proxy-group: tailnet-inbound
spec:
  type: ExternalName
  externalName: dockge-celestia
  ports:
    - name: https
      port: 443
      targetPort: 443
    - name: ssh
      port: 22
      targetPort: 22
---
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: dockge-celestia-ssh
spec:
  interval: 5m
  module: ssh_banner
  prober:
    url: blackbox-exporter.observability.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - dockge-celestia.${TAILSCALE_DOMAIN}:22
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: dockge-celestia-alerts
spec:
  groups:
    - name: dockge-celestia
      rules:
        - alert: DockgeSSHConnectivityLost
          annotations:
            description: "SSH connectivity to Dockge on celestia has been lost."
            summary: "Dockge celestia SSH lost"
          expr: |
            probe_success{probe="dockge-celestia-ssh"} < 1
          for: 10m
          labels:
            severity: warning
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/refs/heads/master/v1.34.2/service.json
apiVersion: v1
kind: Service
metadata:
  name: proxmox-celestia
  annotations:
    tailscale.com/tailnet-fqdn: "celestia.${TAILSCALE_DOMAIN}"
    tailscale.com/proxy-group: tailnet-inbound
spec:
  type: ExternalName
  externalName: celestia
  ports:
    - name: pve
      port: 8006
      targetPort: 8006
    - name: ssh
      port: 22
      targetPort: 22
---
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: proxmox-celestia
spec:
  interval: 5m
  module: http_2xx
  prober:
    url: blackbox-exporter.observability.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - https://celestia.${TAILSCALE_DOMAIN}:8006
---
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: proxmox-celestia-ssh
spec:
  interval: 5m
  module: ssh_banner
  prober:
    url: blackbox-exporter.observability.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - celestia.${TAILSCALE_DOMAIN}:22
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: proxmox-celestia-alerts
spec:
  groups:
    - name: proxmox-celestia
      rules:
        - alert: ProxmoxServiceUnhealthy
          annotations:
            description: "Proxmox VE on celestia is unhealthy."
            summary: "Proxmox celestia is unhealthy"
          expr: |
            probe_success{probe="proxmox-celestia"} < 1
          for: 10m
          labels:
            severity: warning
        - alert: ProxmoxSSHConnectivityLost
          annotations:
            description: "SSH connectivity to Proxmox on celestia has been lost."
            summary: "Proxmox celestia SSH lost"
          expr: |
            probe_success{probe="proxmox-celestia-ssh"} < 1
          for: 10m
          labels:
            severity: warning
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/refs/heads/master/v1.34.2/service.json
apiVersion: v1
kind: Service
metadata:
  name: pbs-celestia
  annotations:
    tailscale.com/tailnet-fqdn: "pbs-celestia.${TAILSCALE_DOMAIN}"
    tailscale.com/proxy-group: tailnet-inbound
spec:
  type: ExternalName
  externalName: pbs-celestia
  ports:
    - name: pbs
      port: 8007
      targetPort: 8007
    - name: ssh
      port: 22
      targetPort: 22
---
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: pbs-celestia
spec:
  interval: 5m
  module: http_2xx
  prober:
    url: blackbox-exporter.observability.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - https://pbs-celestia.${TAILSCALE_DOMAIN}:8007
---
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: pbs-celestia-ssh
spec:
  interval: 5m
  module: ssh_banner
  prober:
    url: blackbox-exporter.observability.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - pbs-celestia.${TAILSCALE_DOMAIN}:22
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: pbs-celestia-alerts
spec:
  groups:
    - name: pbs-celestia
      rules:
        - alert: PBSSSHConnectivityLost
          annotations:
            description: "SSH connectivity to PBS on celestia has been lost."
            summary: "PBS celestia SSH lost"
          expr: |
            probe_success{probe="pbs-celestia-ssh"} < 1
          for: 10m
          labels:
            severity: warning
