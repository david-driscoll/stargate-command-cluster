---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app tdarr-node
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
  dependsOn: []
  interval: 15m
  values:
    controllers:
      *app :
        type: deployment
        replicas: 3
        containers:
          app:
            image:
              repository: ghcr.io/haveagitgat/tdarr_node
              tag: 2.63.01@sha256:1617291d92b35aa1ec2833ab8286e7a85c56945f917132f14af2dc060ae4fc51
            env:
              TZ: ${TIMEZONE}
              PUID: 1000
              PGID: 1000
              serverURL: http://tdarr-server.${CLUSTER_CNAME}.svc.cluster.local:8266
              inContainer: true
              nodeName:
                valueFrom:
                  fieldRef:
                    fieldPath: spec.nodeName
            resources:
              requests:
                cpu: 100m
                memory: 512Mi
                gpu.intel.com/i915: 2
              limits:
                memory: 2Gi
                gpu.intel.com/i915: 2
        pod:
          affinity:
            nodeAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                nodeSelectorTerms:
                - matchExpressions:
                  - key: intel.feature.node.kubernetes.io/gpu
                    operator: In
                    values:
                    - "true"
    defaultPodOptions:
      securityContext:
        # Let the image entrypoint (s6) run as root and drop to PUID/PGID itself; forcing
        # runAsUser prevented it from setting supplemental groups and triggered
        # "s6-applyuidgid: fatal: unable to set supplementary group list: Operation not permitted".
        supplementalGroups: [568]
        # seccompProfile: { type: RuntimeDefault }
    persistence:
      media:
        existingClaim: truenas-media
        globalMounts:
          - path: /media/
