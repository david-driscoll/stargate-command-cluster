---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: k8s-gateway
  namespace: network
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: k8s-gateway
