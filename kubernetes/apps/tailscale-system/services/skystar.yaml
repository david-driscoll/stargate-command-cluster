# Generated by Update.cs — do not edit manually
# Server: skystar  |  Services: Dockge, Proxmox
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/refs/heads/master/v1.34.2/service.json
apiVersion: v1
kind: Service
metadata:
  name: dockge-skystar
  annotations:
    tailscale.com/tailnet-fqdn: "dockge-skystar.${TAILSCALE_DOMAIN}"
    tailscale.com/proxy-group: tailnet-inbound
spec:
  type: ExternalName
  externalName: dockge-skystar
  ports:
    - name: https
      port: 443
      targetPort: 443
    - name: ssh
      port: 22
      targetPort: 22
---
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: dockge-skystar-https
spec:
  interval: 5m
  module: http_2xx
  prober:
    url: blackbox-exporter.observability.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - https://dockge-skystar.${TAILSCALE_DOMAIN}
---
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: dockge-skystar-ssh
spec:
  interval: 5m
  module: ssh_banner
  prober:
    url: blackbox-exporter.observability.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - dockge-skystar.${TAILSCALE_DOMAIN}:22
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: dockge-skystar-alerts
spec:
  groups:
    - name: dockge-skystar
      rules:
        - alert: DockgeServiceUnhealthy
          annotations:
            description: "Dockge service on skystar is unhealthy."
            summary: "Dockge skystar is unhealthy"
          expr: |
            probe_success{probe="dockge-skystar-https"} < 1
          for: 10m
          labels:
            severity: warning
        - alert: DockgeSSHConnectivityLost
          annotations:
            description: "SSH connectivity to Dockge on skystar has been lost."
            summary: "Dockge skystar SSH lost"
          expr: |
            probe_success{probe="dockge-skystar-ssh"} < 1
          for: 10m
          labels:
            severity: warning
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/refs/heads/master/v1.34.2/service.json
apiVersion: v1
kind: Service
metadata:
  name: proxmox-skystar
  annotations:
    tailscale.com/tailnet-fqdn: "skystar.${TAILSCALE_DOMAIN}"
    tailscale.com/proxy-group: tailnet-inbound
spec:
  type: ExternalName
  externalName: skystar
  ports:
    - name: pve
      port: 8006
      targetPort: 8006
    - name: ssh
      port: 22
      targetPort: 22
---
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: proxmox-skystar
spec:
  interval: 5m
  module: http_2xx
  prober:
    url: blackbox-exporter.observability.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - https://skystar.${TAILSCALE_DOMAIN}:8006
---
apiVersion: monitoring.coreos.com/v1
kind: Probe
metadata:
  name: proxmox-skystar-ssh
spec:
  interval: 5m
  module: ssh_banner
  prober:
    url: blackbox-exporter.observability.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - skystar.${TAILSCALE_DOMAIN}:22
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: proxmox-skystar-alerts
spec:
  groups:
    - name: proxmox-skystar
      rules:
        - alert: ProxmoxServiceUnhealthy
          annotations:
            description: "Proxmox VE on skystar is unhealthy."
            summary: "Proxmox skystar is unhealthy"
          expr: |
            probe_success{probe="proxmox-skystar"} < 1
          for: 10m
          labels:
            severity: warning
        - alert: ProxmoxSSHConnectivityLost
          annotations:
            description: "SSH connectivity to Proxmox on skystar has been lost."
            summary: "Proxmox skystar SSH lost"
          expr: |
            probe_success{probe="proxmox-skystar-ssh"} < 1
          for: 10m
          labels:
            severity: warning
